import type { ReactiveRpcMessage } from '../messages';
import type { JsonValueCodec } from '../../../json-pack/codecs/types';
import type { RpcMessageCodec } from './types';
export declare class RpcCodec {
    readonly valueCodec: JsonValueCodec;
    readonly messageCodec: RpcMessageCodec;
    constructor(valueCodec: JsonValueCodec, messageCodec: RpcMessageCodec);
    encode(messages: ReactiveRpcMessage[]): Uint8Array;
    decode(data: Uint8Array): ReactiveRpcMessage[];
}
